# =============================================================================
# PPE Monitoring System — Dependencies
# =============================================================================
# Установка: pip install -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# CORE: Computer Vision & Deep Learning
# -----------------------------------------------------------------------------
# YOLOv8 framework — детекция людей и СИЗ
ultralytics>=8.0.0

# PyTorch — бэкенд для инференса
# Для AMD/CPU: устанавливай без CUDA (это версия по умолчанию)
# Для NVIDIA: замени на: torch==2.1.0+cu118 --extra-index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
torchvision>=0.15.0

# OpenCV — работа с видео и отрисовка
# ВАЖНО: НЕ используй opencv-python-headless, так как нужен cv2.imshow
opencv-python>=4.8.0

# NumPy — работа с массивами и тензорами
numpy>=1.24.0

# -----------------------------------------------------------------------------
# NETWORKING: API-коммуникация с дашбордом
# -----------------------------------------------------------------------------
# HTTP-клиент для отправки нарушений на сервер
requests>=2.31.0

# Low-level HTTP библиотека (нужна для retry-логики в urllib3)
urllib3>=2.0.0

# -----------------------------------------------------------------------------
# SYSTEM: Авто-оптимизация под железо
# -----------------------------------------------------------------------------
# Мониторинг ресурсов (CPU ядра, RAM) для hardware.py
# Опционально: без него код работает, но с меньшей точностью
psutil>=5.9.0

# -----------------------------------------------------------------------------
# UTILITIES: Вспомогательные библиотеки
# -----------------------------------------------------------------------------
typing-extensions>=4.5.0; python_version < "3.12"lap==0.5.13
onnx==1.21.0
onnxruntime==1.26.0
